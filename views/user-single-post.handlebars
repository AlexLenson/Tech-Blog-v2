<div class="columns is-centered mt-6">
    <div class="column is-6">
        <div class="card">
            <div class="card-header">
                <textarea class="textarea" id="upTitle">{{posts.title}}</textarea>
            </div>
            <div class="card-content">
                <textarea class="textarea" id="upBody"> {{posts.content}}</textarea>
    
            </div>
            <footer class="card-footer">
                <!---ADD IDS ON A TAGS THAT ARE IN EDIT.JS----->
                {{!-- added classes/ids but its not updating? --}}
                <a href="#" data-id="{{posts.id}}" class="card-footer-item upPost">Edit</a>
                <a href="#" data-id="{{posts.id}}" class="card-footer-item delPost">Delete</a>
            </footer>
        </div>
    </div>


</div>

<script src="/js/edit.js"></script>